# Install PyTorch with the CUDA build matching your system, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch
torchvision
numpy
Pillow
PyYAML

# CARLA client library must match the server version (0.9.16):
#   pip install carla==0.9.16
